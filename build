#!/usr/bin/env bash
set -e

if [ $# -gt 0 ]
then
    if [ "$1" == "model" ]
    then
        _build/model.py $2
        exit
    elif [ "$1" == "test" ]
    then
        _build/004-test
        exit
    fi
fi

for build_script in $(ls -1 _build/0*)
do
    source $build_script "$*"
done
