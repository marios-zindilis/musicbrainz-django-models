machine:
  python:
    version: 3.6.1
dependencies:
  pre:
    - pip install django
test:
  pre:
    - ./manage.py migrate
  override:
    - coverage run manage.py test musicbrainz_django_models.tests
